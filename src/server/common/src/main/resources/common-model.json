{
  "openapi": "3.0.0",
  "info": {},
  "components": {
    "schemas": {
      "ErrorType": {
        "description": "Error type",
        "type": "string",
        "enum": [
          "CONFIGURATION_ERROR",
          "INVALID_REQUEST",
          "REQUEST_REJECTED",
          "INTEGRATION_ERROR",
          "UNEXPECTED_ERROR"
        ]
      }
    }
  }
}